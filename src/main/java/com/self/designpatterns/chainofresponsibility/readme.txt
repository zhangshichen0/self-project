责任链模式：

javax.servlet.filter-->Filter

作用：
    使多个对象都有机会处理请求，从而避免了请求发出者和处理着之间的耦合。将这些对象连成一条链，请求沿着这条链进行传递，知道有一个对象处理它。

构成：
    handler：定义处理器接口，并实现后续链

代码结构：
    common中是普遍的方式
    filter中为仿照filter的方式写的


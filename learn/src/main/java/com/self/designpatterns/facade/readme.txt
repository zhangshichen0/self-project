门面模式

作用：
    提供一个统一的接口，用来访问子系统中的一群接口，从而让子系统更容易使用。

构成：
    Facade:集合了所有子系统的统一接口
    subSystem:子系统


作用：
    最少知道原则，客户对象所需要交互的对象应尽可能的少，调用者只需要知道依赖了这一个统一接口类，而不需要关心真正调用的类

例子：
    elastic-job-》JobFacade，SchedulerFacade